@* ------------------------------------------------------------------------------------------------------------------ *@
@* Imports *@
@* ------------------------------------------------------------------------------------------------------------------ *@
@using InfiniLore.InfiniBlazor.Components.CollapsibleMenu
@using InfiniLore.InfiniBlazor.Themes
@* ------------------------------------------------------------------------------------------------------------------ *@
@* Descriptors
@* ------------------------------------------------------------------------------------------------------------------ *@
@rendermode InteractiveServer
@inject IThemeSelector ThemeSelector
@* ------------------------------------------------------------------------------------------------------------------ *@
@* Html *@
@* ------------------------------------------------------------------------------------------------------------------ *@
<CollapsibleMenu @bind-IsMenuCollapsed="@IsMenuCollapsed" Width="w-64">
    <Header>
        <a href="/"
           class="@(!IsMenuCollapsed ? "text-1xl font-bold text-white hover:text-gray-300 no-underline" : "hidden")">
            InfiniLore.InfiniBlazor
        </a>
    </Header>

    <NavigationMenu>
        <CollapsibleMenuButton Href="/markdown-dual">
            <Icon> <LiSignature/></Icon>
            <Text>Markdown Example</Text>
        </CollapsibleMenuButton>
        <CollapsibleMenuButton Href="/markdown-rolling">
            <Icon><LiIndentIncrease/></Icon>
            <Text>Rolling Markdown Example</Text>
        </CollapsibleMenuButton>
    </NavigationMenu>

    <Footer>
        <CollapsibleMenuButton Href="https://github.com/InfiniLore/infinilore.infiniblazor">
            <Icon><LiPackage/></Icon>
            <Text>Github</Text>
        </CollapsibleMenuButton>
        <div>
            <button @onclick="SwitchTheme" class="bg-red-600" >THEME</button>
            <button @onclick="ThemeSelector.ToggleMode" class="bg-green-600"> MODE </button>
            
        </div>
    </Footer>
</CollapsibleMenu>

@* ------------------------------------------------------------------------------------------------------------------ *@
@* Code *@
@* ------------------------------------------------------------------------------------------------------------------ *@
@code {
    private string Theme { get; set; } = "default";
    private bool IsMenuCollapsed { get; set; } = false; // Tracks whether the menu is collapsed
    private void SwitchTheme() {
        Theme = Theme switch {
            "default" => "anna",
            "anna" => "default",
            _ => Theme
        };
        ThemeSelector.TrySelectTheme(Theme);
    }
    private void switchMode() {
        throw new NotImplementedException();
    }
}
