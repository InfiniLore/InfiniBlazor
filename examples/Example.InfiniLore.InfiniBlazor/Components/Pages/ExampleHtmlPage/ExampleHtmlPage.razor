@* ------------------------------------------------------------------------------------------------------------------ *@
@* Imports
@* ------------------------------------------------------------------------------------------------------------------ *@
@using Infinilore.InfiniBlazor
@* ------------------------------------------------------------------------------------------------------------------ *@
@* Descriptors
@* ------------------------------------------------------------------------------------------------------------------ *@
@page "/html"
@rendermode InteractiveServer
@* ------------------------------------------------------------------------------------------------------------------ *@
@* Html
@* ------------------------------------------------------------------------------------------------------------------ *@
<InfiniPage>
    <InfiniSection>
        <InfiniToggleButton
            @bind-Value="isEnabled"
            OnIcon="sun"
            OffIcon="moon"
            Size="Size.Xxs"
            Tooltip="Toggle theme"
        />
        <InfiniToggleButton
            @bind-Value="isEnabled"
            OnIcon="sun"
            OffIcon="moon"
            Size="Size.Xs"
            Tooltip="Toggle theme"
        />
        <InfiniToggleButton
            @bind-Value="isEnabled"
            OnIcon="sun"
            OffIcon="moon"
            Size="Size.S"
            Tooltip="Toggle theme"
        />
        <InfiniToggleButton
            @bind-Value="isEnabled"
            OnIcon="sun"
            OffIcon="moon"
            Size="Size.M"
            Tooltip="Toggle theme"
        />
        <InfiniToggleButton
            @bind-Value="isEnabled"
            OnIcon="sun"
            OffIcon="moon"
            Size="Size.L"
            Tooltip="Toggle theme"
        />
        <InfiniToggleButton
            @bind-Value="isEnabled"
            OnIcon="sun"
            OffIcon="moon"
            Size="Size.Xl"
            Tooltip="Toggle theme"
        />
        <InfiniToggleButton
            @bind-Value="isEnabled"
            OnIcon="sun"
            OffIcon="moon"
            Size="Size.Xxl"
            Tooltip="Toggle theme"
        />
    </InfiniSection>

    <SectionTextInput/>

    <SectionCodeBlock/>

    <SectionHeadings/>

    <SectionSpacers/>
</InfiniPage>
@* ------------------------------------------------------------------------------------------------------------------ *@
@* Code
@* ------------------------------------------------------------------------------------------------------------------ *@

@code {
    bool isEnabled;
}
